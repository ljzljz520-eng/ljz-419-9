# 本服务级 .env 为可选项：复制根目录 .env.example 为根目录 .env 即可完成首次配置。
# 本文件若存在，优先级高于根 .env（真实环境变量优先级最高）。
PORT=3002
DATABASE_URL=postgres://hospital:hospital@localhost:5433/department_service
# 必须与 account-service 一致，用于验证其签发的 JWT
JWT_SECRET=change-me-in-production-please
LOG_LEVEL=info
